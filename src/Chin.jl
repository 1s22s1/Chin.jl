module Chin
    function oneAutomaton(character)
        [
            0 character
            0 0
        ]
    end

    function  connectionAutomaton(s, t)
        # f = t[1, end]
        # A = zeros(Int, ndims(s))
        # A[end] = f

        # B = [s, A]

        # B
    end
end
